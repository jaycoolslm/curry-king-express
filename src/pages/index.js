import * as React from "react"
import TopBar from "../components/TopBar"


const page = {
  height: '200vh'
}

const LandingPage = () => {
  return (
    <main style={page}>
      <TopBar />
    </main>
  )
}

export default LandingPage